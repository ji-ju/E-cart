@model IEnumerable<Newproj.Models.Profession>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProfessionName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProfessionName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProfId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
